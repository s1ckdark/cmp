import Breadcrumb from "@/components/Breadcrumb";
import { Tables } from "@/components/Tables";

const SupportList = () => {
    return (
        <>
            <Breadcrumb />
            <Tables rowType={'support'} />
        </>
    );
}
export default SupportList;